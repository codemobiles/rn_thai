import {View, Text} from 'react-native';
import React from 'react';

type Props = {};

const CMButton = (props: Props) => {
  return (
    <View>
      <Text>CMButton</Text>
    </View>
  );
};

export default CMButton;
